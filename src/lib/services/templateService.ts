/**
 * Template Service
 * Firestore CRUD for email and SMS templates
 *
 * TODO: Implement
 * - getAll(type?)
 * - getById(id)
 * - create(data)
 * - update(id, data)
 * - delete(id)
 * - duplicate(id)
 */

export { };

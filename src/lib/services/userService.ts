/**
 * User Service
 * Firestore CRUD for admin/manager user records
 *
 * TODO: Implement
 * - getAll()
 * - getById(id)
 * - create(data)         // Invite user
 * - update(id, data)
 * - delete(id)
 * - updateRole(id, role)
 * - getCurrentUser()
 * - getLogs(filters?)
 * - createLog(entry)
 */

export { };

/**
 * Message Service
 * Firestore CRUD for the messages collection
 *
 * TODO: Implement
 * - getAll(filters?)
 * - getById(id)
 * - create(data)
 * - update(id, data)
 * - delete(id)
 * - schedule(id, sendAt)
 * - cancel(id)
 * - getScheduled()
 * - getSent()
 * - createSequence(messages[])
 * - getAnalytics(messageId)
 */

export { };

/**
 * Playbook Service
 * Firestore CRUD for qualification playbook configuration
 *
 * TODO: Implement
 * - getActive()
 * - getById(id)
 * - create(data)
 * - update(id, data)
 * - addStep(playbookId, step)
 * - updateStep(playbookId, stepId, data)
 * - removeStep(playbookId, stepId)
 * - reorderSteps(playbookId, stepIds[])
 */

export { };

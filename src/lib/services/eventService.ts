/**
 * Event Service
 * Firestore CRUD for the events collection
 *
 * TODO: Implement
 * - getAll(filters?)
 * - getById(id)
 * - create(data)
 * - update(id, data)
 * - delete(id)
 * - publish(id)
 * - unpublish(id)
 * - getRegistrations(eventId)
 * - addRegistration(eventId, contactId)
 * - getNotificationSequence(eventId)
 * - updateNotificationSequence(eventId, steps)
 */

export { };

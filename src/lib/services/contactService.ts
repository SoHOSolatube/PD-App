/**
 * Contact Service
 * Firestore CRUD for the contacts collection
 *
 * TODO: Implement
 * - getAll(filters?)
 * - getById(id)
 * - create(data)
 * - update(id, data)
 * - delete(id)
 * - searchByPhone(phone)
 * - getConversationHistory(contactId)
 * - addNote(contactId, note)
 * - updateCategories(contactId, categories)
 */

export { };

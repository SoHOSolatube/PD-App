/**
 * Prospect Service
 * Firestore CRUD for the prospects collection
 *
 * TODO: Implement
 * - getAll(filters?)
 * - getById(id)
 * - create(data)
 * - update(id, data)
 * - delete(id)
 * - updateTags(id, tags)
 * - updateQualificationStatus(id, status)
 * - updatePlaybookStep(id, step)
 * - addCollectedData(id, key, value)
 * - getConversation(prospectId)
 * - addConversationMessage(prospectId, message)
 * - toggleConversationMode(prospectId, mode)
 */

export { };

/**
 * Suppression Service
 * Firestore CRUD for the suppression list (opt-outs)
 *
 * TODO: Implement
 * - getAll()
 * - addOptOut(contactOrProspectId, channel)
 * - removeOptOut(id)
 * - isOptedOut(phone, channel)
 * - isOptedOutEmail(email)
 * - syncFromWebhook(event)   // Twilio/SendGrid webhook data
 */

export { };
